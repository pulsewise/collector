# Pulsewise Collector Configuration
# Copy this file to /etc/pulsewise-collector/config and fill in your values

TOKEN=your-token-here

# Optional — auto-detected from system hostname if omitted
# HOSTNAME=my-server

# Optional — defaults to https://pulsewise.app/api/collect
# URL=http://localhost:8000/api/collect

# Optional — set to false to disable automatic updates (default: true)
# AUTO_UPDATE=false

# Optional — logs full response body from the server
# DEBUG=true

# ─────────────────────────────────────────────────────────────
# Optional integrations
# ─────────────────────────────────────────────────────────────

# PHP-FPM status page URL (requires pm.status_path = /status in php-fpm pool config)
# FPM_STATUS_URL=http://127.0.0.1/fpm-status

# Nginx stub_status URL (requires stub_status module and location block)
# NGINX_STATUS_URL=http://127.0.0.1/nginx-status

# TCP port availability checks — comma-separated list of port numbers
# CHECK_PORTS=22,80,443,3306,6379
